{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  
  "buildCommand": "prisma generate && prisma db push && next build",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
